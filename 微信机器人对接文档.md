# 微信机器人对接开发文档

> 本文档面向微信机器人开发者，描述如何将微信群聊消息对接到理博基金客服智能体平台。

---

## 1. 系统架构概览

```
┌──────────────┐     POST /api/webhook/message      ┌──────────────────────┐
│              │ ──────────────────────────────────→ │                      │
│  微信机器人   │                                     │   客服智能体后端       │
│              │ ←────────────────────────────────── │   (FastAPI)           │
│  (你开发的)   │     GET /api/webhook/pending-messages│                      │
│              │                                     │  ┌─ SQLite 消息存储    │
│              │     POST /api/webhook/message-sent  │  ├─ DeepSeek 意愿分析  │
│              │ ──────────────────────────────────→ │  ├─ 飞书通知管理员      │
└──────────────┘                                     │  └─ 管理后台 /admin    │
                                                     └──────────────────────┘
```

**机器人需要完成的三件事：**

1. **推送消息** — 监听微信群聊，将收到的消息 POST 到后端
2. **拉取跟单** — 定时轮询后端，获取管理员确认后待发送的跟单消息
3. **回调确认** — 跟单消息发送后，回调后端更新发送状态

---

## 2. 后端服务地址

| 环境 | 地址 |
|------|------|
| 本地开发 | `http://localhost:8000` |
| 生产环境 | 部署后替换为实际域名 |

所有 API 路径以 `/api/webhook/` 开头，**无需认证**（Webhook 接口不走管理员 token）。

---

## 3. API 接口详细说明

### 3.1 推送群消息

**接口：** `POST /api/webhook/message`

**说明：** 微信机器人每收到一条群聊消息，调用此接口推送到后端。后端会自动保存消息，并在后台异步分析用户的下单意愿。

**请求头：**

```
Content-Type: application/json
```

**请求体（JSON）：**

```json
{
  "sender_id": "wxid_abc123",
  "sender_name": "张三",
  "group_id": "group_001",
  "group_name": "理博基金VIP投资群",
  "content": "你们的基金怎么购买？起投金额多少？",
  "msg_type": "text",
  "msg_id": "msg_20260311_001",
  "timestamp": "2026-03-11T10:30:00"
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `sender_id` | string | **是** | 消息发送者的唯一标识（微信ID、wxid 等），用于识别客户 |
| `sender_name` | string | 否 | 发送者昵称/群备注名，用于在后台展示，默认空字符串 |
| `group_id` | string | 否 | 群的唯一标识，默认空字符串 |
| `group_name` | string | 否 | 群名称，用于后台展示和飞书通知 |
| `content` | string | **是** | 消息文本内容 |
| `msg_type` | string | 否 | 消息类型，默认 `"text"`。仅文本消息会触发意愿分析 |
| `msg_id` | string | 否 | 消息唯一ID，用于去重。重复的 `msg_id` 不会重复入库 |
| `timestamp` | string | 否 | 消息时间，ISO 8601 格式。不传则使用服务器当前时间 |

**成功响应（200）：**

```json
{
  "status": "ok",
  "message_id": 42,
  "customer_id": 7
}
```

**去重响应（200）：**

```json
{
  "status": "duplicate",
  "message": "消息已存在"
}
```

**错误响应（422）：** 缺少必填字段

```json
{
  "detail": [
    {
      "loc": ["body", "sender_id"],
      "msg": "field required",
      "type": "value_error.missing"
    }
  ]
}
```

---

### 3.2 拉取待发送跟单消息

**接口：** `GET /api/webhook/pending-messages`

**说明：** 机器人定时轮询此接口，获取管理员已确认、AI 已生成的跟单消息。拿到消息后通过微信发送给对应客户。

**请求：** 无参数，直接 GET。

**成功响应（200）：**

```json
{
  "messages": [
    {
      "follow_up_id": 1,
      "target_user_id": "wxid_abc123",
      "target_group_id": "group_001",
      "content": "您好张三，注意到您对我们的基金产品感兴趣...",
      "customer_name": "张三"
    },
    {
      "follow_up_id": 2,
      "target_user_id": "wxid_def456",
      "target_group_id": "",
      "content": "李总您好，关于您咨询的量化选股策略...",
      "customer_name": "李四"
    }
  ]
}
```

**字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `follow_up_id` | int | 跟进记录ID，**发送后回调时必须携带此ID** |
| `target_user_id` | string | 目标客户的微信标识（与推送时的 `sender_id` 一致） |
| `target_group_id` | string | 目标群ID。如果为空，建议发私聊；如果有值，可发到群里 @该用户 |
| `content` | string | AI 生成的跟单消息文本（管理员可能已编辑过） |
| `customer_name` | string | 客户名称，仅供参考 |

**无待发送消息时：**

```json
{
  "messages": []
}
```

---

### 3.3 回调确认消息已发送

**接口：** `POST /api/webhook/message-sent`

**说明：** 机器人成功或失败发送跟单消息后，调用此接口通知后端更新状态。

**请求体（JSON）：**

发送成功：
```json
{
  "follow_up_id": 1,
  "success": true
}
```

发送失败：
```json
{
  "follow_up_id": 1,
  "success": false,
  "error": "用户已退群，无法发送"
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `follow_up_id` | int | **是** | 从 `pending-messages` 接口获取的跟进记录ID |
| `success` | bool | 否 | 是否发送成功，默认 `true` |
| `error` | string | 否 | 失败原因描述，仅 `success=false` 时有意义 |

**成功响应（200）：**

```json
{
  "status": "ok"
}
```

---

## 4. 完整工作流程

```
步骤 1: 机器人监听微信群 → 收到消息
         │
步骤 2: POST /api/webhook/message（推送消息到后端）
         │
步骤 3: 后端自动处理（后台每5秒轮询一次）
         ├── 保存消息到 SQLite
         ├── 调用 DeepSeek 分析下单意愿
         ├── 高/中意向 → 飞书推送通知管理员
         │
步骤 4: 管理员收到飞书通知 → 打开管理后台(/admin)
         ├── 查看客户消息和意愿分析
         ├── 点击"跟进" → AI 生成跟单消息
         ├── 编辑消息(可选) → 确认发送
         │
步骤 5: 机器人轮询 GET /api/webhook/pending-messages
         ├── 获取到待发送消息
         ├── 通过微信发送给对应客户
         │
步骤 6: POST /api/webhook/message-sent（回调确认发送结果）
```

---

## 5. 机器人开发指南

### 5.1 推荐轮询间隔

| 操作 | 建议间隔 | 说明 |
|------|---------|------|
| 推送消息 | 实时 | 收到微信消息立即推送，无需延迟 |
| 拉取待发送 | 5-10 秒 | 跟单消息不要求秒级时效，避免过于频繁请求 |
| 发送回调 | 发送后立即 | 发送成功/失败后立刻回调 |

### 5.2 建议发送策略

- **优先私聊**：如果 `target_group_id` 为空，直接私聊发送
- **群内 @**：如果 `target_group_id` 有值，可以在该群内 @ 该用户发送
- **发送失败处理**：发送失败时一定要调用回调接口报告，以便管理员在后台看到失败原因

### 5.3 消息类型处理

目前后端仅对 `msg_type="text"` 的消息做意愿分析。但建议机器人也推送其他类型的消息用于存档：

| msg_type | 说明 | content 内容 |
|----------|------|-------------|
| `text` | 文本消息 | 消息原文 |
| `image` | 图片消息 | 可传 `[图片消息]` 占位 |
| `link` | 链接消息 | 链接标题或 URL |
| `file` | 文件消息 | 文件名 |

---

## 6. Python 代码示例

### 6.1 完整的机器人框架示例

```python
"""微信机器人对接客服智能体后端的示例代码"""

import time
import requests
from datetime import datetime

# 后端服务地址
API_BASE = "http://localhost:8000"


def push_message(sender_id: str, sender_name: str, group_id: str,
                 group_name: str, content: str, msg_id: str = None):
    """推送一条群消息到后端"""
    payload = {
        "sender_id": sender_id,
        "sender_name": sender_name,
        "group_id": group_id,
        "group_name": group_name,
        "content": content,
        "msg_type": "text",
        "msg_id": msg_id,
        "timestamp": datetime.now().isoformat(),
    }
    try:
        resp = requests.post(f"{API_BASE}/api/webhook/message", json=payload, timeout=10)
        result = resp.json()
        print(f"[推送] {sender_name}: {content[:30]}... → {result['status']}")
        return result
    except Exception as e:
        print(f"[推送失败] {e}")
        return None


def poll_and_send_followups(send_func):
    """
    拉取待发送的跟单消息，逐条发送。

    Args:
        send_func: 实际发送微信消息的函数，签名为:
                   send_func(target_user_id, content, target_group_id="") -> bool
    """
    try:
        resp = requests.get(f"{API_BASE}/api/webhook/pending-messages", timeout=10)
        data = resp.json()
    except Exception as e:
        print(f"[拉取失败] {e}")
        return

    for msg in data.get("messages", []):
        follow_up_id = msg["follow_up_id"]
        target_user = msg["target_user_id"]
        target_group = msg.get("target_group_id", "")
        content = msg["content"]
        name = msg["customer_name"]

        print(f"[跟单] 发送给 {name}({target_user}): {content[:30]}...")

        # 调用你的微信发送函数
        try:
            success = send_func(target_user, content, target_group)
            # 回调确认
            callback = {"follow_up_id": follow_up_id, "success": success}
            if not success:
                callback["error"] = "发送失败"
            requests.post(f"{API_BASE}/api/webhook/message-sent", json=callback, timeout=10)
            print(f"[回调] follow_up_id={follow_up_id} → {'成功' if success else '失败'}")
        except Exception as e:
            # 回调报告错误
            requests.post(f"{API_BASE}/api/webhook/message-sent", json={
                "follow_up_id": follow_up_id,
                "success": False,
                "error": str(e),
            }, timeout=10)
            print(f"[发送异常] {e}")


# ============================================================
# 主循环示例
# ============================================================

def my_wechat_send(target_user_id, content, target_group_id=""):
    """
    你需要实现的微信发送函数。
    返回 True 表示发送成功，False 表示失败。
    """
    # TODO: 调用你的微信机器人SDK发送消息
    # 例如: wechat_bot.send_text(target_user_id, content)
    print(f"  → 发送消息给 {target_user_id}: {content[:50]}...")
    return True


def on_group_message(msg):
    """
    你的微信机器人收到群消息时的回调。
    msg 的结构取决于你使用的微信机器人框架。
    """
    push_message(
        sender_id=msg["sender_wxid"],
        sender_name=msg["sender_name"],
        group_id=msg["group_wxid"],
        group_name=msg["group_name"],
        content=msg["content"],
        msg_id=msg.get("msg_id"),
    )


def main():
    """主循环：持续轮询待发送消息"""
    print("机器人已启动，开始轮询待发送消息...")
    while True:
        poll_and_send_followups(my_wechat_send)
        time.sleep(5)  # 每5秒检查一次


if __name__ == "__main__":
    main()
```

### 6.2 使用 curl 测试

**推送消息：**

```bash
curl -X POST http://localhost:8000/api/webhook/message \
  -H "Content-Type: application/json" \
  -d '{
    "sender_id": "wxid_test001",
    "sender_name": "测试用户",
    "group_id": "group_test",
    "group_name": "测试群",
    "content": "你们的基金怎么购买？最低多少钱？",
    "msg_type": "text",
    "msg_id": "test_msg_001"
  }'
```

**拉取待发送消息：**

```bash
curl http://localhost:8000/api/webhook/pending-messages
```

**回调确认发送：**

```bash
curl -X POST http://localhost:8000/api/webhook/message-sent \
  -H "Content-Type: application/json" \
  -d '{"follow_up_id": 1, "success": true}'
```

---

## 7. 意愿识别说明

后端收到消息后，会在后台使用 DeepSeek 大模型自动分析客户的下单意愿，分为四个等级：

| 等级 | 分数范围 | 触发飞书通知 | 典型消息示例 |
|------|---------|-------------|------------|
| **high** (高) | 0.7 - 1.0 | ✅ | "怎么购买？""起投多少钱？""开放日是哪天？" |
| **medium** (中) | 0.4 - 0.69 | ✅ | "今年收益怎么样？""你们团队背景？""资金安全吗？" |
| **low** (低) | 0.1 - 0.39 | ❌ | "你们公司在哪？""最近市场怎么看？" |
| **none** (无) | 0.0 - 0.09 | ❌ | "早上好""哈哈""[表情]" |

- 分析过程完全异步，不影响消息推送接口的响应速度
- 后台每 5 秒检查一次新消息并触发分析
- 高/中意向消息会自动通过飞书 Webhook 推送通知管理员

---

## 8. 注意事项

### 8.1 幂等性

- 相同 `msg_id` 的消息只会入库一次，重复推送会返回 `"status": "duplicate"`
- 建议为每条消息生成唯一的 `msg_id` 以确保去重

### 8.2 性能

- 推送消息接口响应时间 < 50ms（仅数据库写入，不做分析）
- 拉取待发送接口响应时间 < 20ms
- 意愿分析在后台异步处理，通常 3-10 秒完成

### 8.3 消息长度

- `content` 字段无硬性长度限制
- 但意愿分析时会取前 500 字进行分析
- 飞书通知中消息内容最多显示 500 字

### 8.4 并发安全

- 多个机器人实例可以同时推送消息，数据库层面有去重机制
- 拉取待发送消息没有锁机制，建议**只运行一个拉取实例**避免重复发送
- 如果需要多实例拉取，应在机器人侧实现分布式锁

### 8.5 错误处理建议

```python
# 推送消息失败时，建议重试
import time

def push_with_retry(payload, max_retries=3):
    for attempt in range(max_retries):
        try:
            resp = requests.post(f"{API_BASE}/api/webhook/message",
                               json=payload, timeout=10)
            if resp.status_code == 200:
                return resp.json()
        except requests.RequestException:
            pass
        if attempt < max_retries - 1:
            time.sleep(2 ** attempt)  # 指数退避: 1s, 2s, 4s
    return None
```

---

## 9. 管理后台

管理员可以访问 `http://<服务器地址>/admin` 查看线索和管理跟进。

- **默认密码**：`admin123`（可在 `.env` 文件中修改 `ADMIN_PASSWORD`）
- **功能**：
  - 仪表盘：今日消息数、高意向线索、待跟进、已发送统计
  - 客户线索：按意愿分数排序，支持高/中意向筛选
  - 跟进管理：AI 生成跟单消息，管理员可编辑后确认发送
  - 消息记录：查看所有群消息历史

---

## 10. 快速验证流程

1. **启动后端服务**（确保 `.env` 中配置了 `DEEPSEEK_API_KEY`）

2. **模拟推送一条高意向消息**
   ```bash
   curl -X POST http://localhost:8000/api/webhook/message \
     -H "Content-Type: application/json" \
     -d '{"sender_id":"test_wx_001","sender_name":"测试客户","group_id":"g1","group_name":"投资交流群","content":"我想购买你们的基金产品，最低起投多少钱？开放日是什么时候？"}'
   ```

3. **等待 5-10 秒**（后台自动分析意愿 + 发送飞书通知）

4. **打开管理后台** `http://localhost:8000/admin`，用密码 `admin123` 登录

5. **在"客户线索"tab** 看到该客户显示为高意向

6. **点击"跟进"** → AI 生成跟单消息 → 编辑确认发送

7. **拉取待发送消息**
   ```bash
   curl http://localhost:8000/api/webhook/pending-messages
   ```
   应返回一条待发送消息

8. **回调确认发送成功**
   ```bash
   curl -X POST http://localhost:8000/api/webhook/message-sent \
     -H "Content-Type: application/json" \
     -d '{"follow_up_id":1,"success":true}'
   ```
